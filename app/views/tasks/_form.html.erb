<%= form_with(model: task) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
        <% task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<!---
  <div class="field">
    <%= form.label :registerdate %>
    <%= form.date_field :registerdate %>
  </div>
  --->

  <table>
    <tr>
      <td><%= form.label :duedate %></td>
      <td><%= form.date_field :duedate %></td>
    </tr>
    <tr>
      <td><%= form.label :user_id %></td>
      <td><%= form.select :user_id, User.all.to_a.map{|i| [i.name, i.id]}, include_blank: "選択してください"%></td>
    </tr>
  </table>

  <!--<div class="field">
	<input class="ef" type="text" placeholder="">
  </div>-->
  <div class="cp_iptxt">
    <%= form.text_field :task, class:"ef", placeholder:"" %>
    <%= form.label :task %>
	<span class="focus_line"></span>
  </div>

  <div class="actions">
    <%= form.submit '作成', class:"form-flat-border" %>
  </div>
<% end %>
