<%= form_with(model: task) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
        <% task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<!---
  <div class="field">
    <%= form.label :registerdate %>
    <%= form.date_field :registerdate %>
  </div>
  --->

  <div class="field">
    <%= form.label :duedate %>
    <%= form.date_field :duedate %>
  </div>

  <div class="field">
    <%= form.label :task %>
    <%= form.text_field :task %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.select :user_id, User.all.to_a.map{|i| [i.name, i.id]}, include_blank: "選択してください"%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
